import 'package:flutter/material.dart';

class CodeViewerSampleFile extends StatelessWidget {
  const CodeViewerSampleFile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Image.asset("assets/images/logo.png")
    );
  }
}
