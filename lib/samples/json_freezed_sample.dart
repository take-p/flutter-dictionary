import 'package:flutter/material.dart';

class JsonFreezedSample extends StatelessWidget {
  const JsonFreezedSample({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
