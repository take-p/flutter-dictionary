import 'package:flutter/material.dart';

class ExpansionPanelSample extends StatelessWidget {
  const ExpansionPanelSample({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
