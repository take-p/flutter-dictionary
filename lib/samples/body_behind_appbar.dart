import 'package:flutter/material.dart';

class BodyBehindAppBar extends StatelessWidget {
  const BodyBehindAppBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("body behind app bar");
  }
}
