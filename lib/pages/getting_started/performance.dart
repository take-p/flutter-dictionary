import 'package:flutter/material.dart';

class PerformanceImprovements extends StatelessWidget {
  const PerformanceImprovements({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("パフォーマンス改善"),),
      body: const Text("ああああ"),
    );
  }
}
